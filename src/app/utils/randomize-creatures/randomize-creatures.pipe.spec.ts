import { RandomizeCreaturesPipe } from './randomize-creatures.pipe';

describe('RandomizeCreaturesPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomizeCreaturesPipe();
    expect(pipe).toBeTruthy();
  });
});
